# v2 Case Study — Data Audit (2026-07-09)

Status of every v2 data stream, refreshed today. "Fresh" = current to 2026-07-09; "auto" = self-collecting
via cron; "local-only" = held on the fleet, not committed (size/policy).

## Judge study (B vs C vs C′ quality)
| Round | judges | n | file | status |
|---|---|---|---|---|
| R2/R3 | 12 | 90-obs frozen | `eval-rank-r2r3-20260621.csv` | committed |
| R4 | 7 (NIM) | 80/regime | `eval-rank-r4-20260621.csv` | committed |
| **R5 (arc per-obs)** | **4 complete + deepseek partial** | **65/regime, 260 trios** | `eval-rank-r5-20260709.csv` | **committed today** |

R5 verdict: arc regime has the LOWEST per-obs quality (ranked last 42.3%, p=0.0027); Stop>arc p=0.0035,
inc>arc p=0.041 — all three contrasts significant. See `THREE-WAY-REGIMES.md` §ROUND 5.

## Regime volumetry / attribution (REFRESHED today)
Generated by `scripts/export-regime-comparison.mjs` (filter updated to include `C-prime-arc`). Reads
`claude_mem` PG on `.253`, project `2e157557`. Now current to 2026-07-09 (was stale at 2026-07-02).
- `regime-daily-volumetry.csv` — daily self-author vs pipeline, through 07-09 (115 days).
- `regime-type-distribution.csv` — self-author avg 2201 chars vs pipeline 1359; self-author 0 redundant
  titles vs pipeline 5586; pattern=85/architecture=28 (arc regime now visible).
- `regime-comparison-metrics.csv`, `regime-pipeline-redundancy.csv` — refreshed.
- `regime-attribution-20260709.csv` — fresh snapshot (supersedes `-20260702`): B(pipeline) 123,301 (0 sid,
  0.6% pat/arch), C 1,282 (4.9%), C-prime 313 (0.6%), **C-prime-arc 66 (72.7%)**, untagged-sa 592.

## Auto-collecting streams (fresh, cron-driven)
- **Observations**: 125,546 total in `claude_mem` .253; last 2026-07-09. +16,932 since 07-02.
- **Latency benchmarks** (`benchmarks` table, claude_telemetry .253): last 2026-07-09; +235 since 07-02.
  Snapshots: `benchmarks-raw.csv`, `benchmarks-by-version.csv` (committed, but as of jun — re-export pending).
- **Behavioral evals** (`evals` table): last 2026-07-09 05:00 (daily cron); +96 since 07-02. Writeup:
  `MODEL-DEGRADATION-EVAL.md`.

## local-only / by policy
- **GCM metrics** (v2 window May 18–Jun 8, ~1.37M datapoints): exported once, held locally, NOT committed
  (size + publication policy). One-time snapshot — "done", not an ongoing stream for v2.

## Open items (honest)
1. ✅ RESOLVED 2026-07-09 — `benchmarks-raw.csv` (3011 rows), `benchmarks-by-version.csv` (119), and a fresh
   `benchmark-model-daily-20260709.csv` (198) re-exported from the live table, current to 07-09.
2. ✅ RESOLVED 2026-07-09 — `push-to-gcm.sh` (.253) works (802 `cc_` metrics, token OK, test push OK) but its
   `*/10` cron had DROPPED: last auto-run 07-07 22:50, ~2-day gap. Cron re-added + verified. Only pushes `cc_`
   metrics (CC/LLM telemetry, ~$0 free-tier); does not affect committed v2 datasets (GCM is local-only).
   ⚠️ Recurring: the cron has now been lost twice — worth a durable guard if it keeps dropping.
3. Branch `data/v2-preliminary` is LOCAL (no push) — durability lives on `.254` only. Push/backup before
   relying on it off-machine.
4. Judge panel could not reach the full 7 judges today (both Llama models emit reasoning-only, no JSON parse);
   4 complete + deepseek partial. Does not change the R5 conclusion.
5. Overarching caveat: all regime findings are PROXIES (LLM-judge quality, kind-mix, volumetry) — downstream
   UTILITY (did injected memory prevent a bug / speed a task?) is still unmeasured.
